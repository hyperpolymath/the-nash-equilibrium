= LLM Warmup — the-nash-equilibrium (User)

== What is the-nash-equilibrium?

See `README.adoc` for overview.

== Key Commands

* `just deps` — fetch dependencies
* `just build` — build the project
* `just test` — run tests
* `just doctor` — diagnose issues
* `just quality` — fmt-check + lint + docs-check + test

== Quick Context

* License: AGPL-3.0-or-later
* Part of hyperpolymath ecosystem
* See `EXPLAINME.adoc` for architecture
