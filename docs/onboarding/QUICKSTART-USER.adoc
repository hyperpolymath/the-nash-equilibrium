// SPDX-License-Identifier: CC-BY-SA-4.0
// Template: QUICKSTART-USER.adoc — 5-minute path to working software
// Replace rsr-template-repo, The Nash Equilibrium — a 6X strategy board game exploring economic systems, planetary boundaries, and post-scarcity futures. See README.adoc for details., just run, Rsr Template Repo started successfully. with actuals
= the-nash-equilibrium — Quick Start for Users
:toc:
:toclevels: 2

== What is the-nash-equilibrium?

The Nash Equilibrium — a 6X strategy board game exploring economic systems, planetary boundaries, and post-scarcity futures. See README.adoc for details.

== Prerequisites

Before you begin, ensure you have:

* **just** — task runner (https://github.com/casey/just[install guide])
* Platform-specific requirements listed below

[cols="1,3"]
|===
| Platform | Additional Requirements

| Linux
| See README.adoc

| macOS
| See README.adoc

| Windows
| See README.adoc
|===

== Install

=== Option 1: Standard Install (recommended)

[source,bash]
----
# Clone and set up
git clone https://github.com/hyperpolymath/the-nash-equilibrium.git
cd the-nash-equilibrium
just deps           # fetch dependencies
just doctor         # self-diagnostic
----

=== Option 2: Container

[source,bash]
----
podman build -f container/Containerfile -t the-nash-equilibrium .
----

== First Run

[source,bash]
----
just run
----

This builds the project and launches it. The run recipe is still being
instantiated from the template — check `just doctor` and the Justfile if
output is not what you expect.

== Self-Diagnostic

If something isn't working:

[source,bash]
----
just doctor
----

This checks all dependencies, permissions, paths, and connectivity.
If it finds issues, it will suggest fixes.

== Get Help

* **In-app**: `just run --help`
* **Guided tour**: `just tour`
* **Report a problem**: `just help-me` (pre-fills diagnostic context)
* **Repository**: https://github.com/hyperpolymath/the-nash-equilibrium

== Uninstall

[source,bash]
----
just uninstall
----

You will be asked:

1. Which uninstall tier (Bennett reversible, parameter-based, standard, or secure)
2. Whether to include or exclude your data
3. Whether to clear caches and LLM models

== Next Steps

* Read the link:README.adoc[README] for full feature overview
* Read the link:EXPLAINME.adoc[EXPLAINME] for architecture and design decisions
* Try `just tour` for a guided walkthrough
